/**
 * localStorage keys (keep stable so existing users keep progress).
 */
(function (global) {
  global.DSA_KEYS = {
    SHEET_PROGRESS: 'dsa_full_v1',
    PRACTICE_PROGRESS: 'dsa_prac_v1',
    STREAK: 'dsa_streak_v1'
  };
})(window);
